consideraciones

*Tener en cuenta las dimensiones de las matrices (en este caso se opto por solo tener en cuenta matrices de igual dimension)
*En este caso el algoritmo de strassen es usado solamente en matrices pares
*Recordar que en este caso la entrada es atravez de un archivo txt llamado "input.txt", modificar este para leer las 2 matrices en cuestion de estudio
*la primera linea de input debe ser las dimensiones de las matrices